level:
  base: 100
  percent-increase: 10
  cap: 50000

xp:
  allowed-worlds: ['bskyblock_world']
  recent-ttl-ms: 5000
  blocks-cache-check-ticks: 200

storage:
  debounce-ticks: 20

ranking:
  refresh-ticks: 6000
  tab-prefix: "&7[ &a섬 랭킹 &f<rank>위 &7| &bLv.<level> &7] &r"

hud:
  bossbar:
    duration-ticks: 40
    step-ticks: 10

upgrade:
  size:
    base-range: 50           # 시작 보호반경
    per-level: 10            # 단계당 증가
    cost-base: 50000         # 첫 업글 비용
    cost-multiplier: 1.25    # 단계마다 비용 증가
    required-level-base: 3   # 첫 업글 요구 레벨
    required-level-step: 2   # 업글마다 추가 요구 레벨
  team:
    base-members: 4
    per-level: 1
    cost-base: 80000
    cost-multiplier: 1.35
    required-level-base: 5
    required-level-step: 2
market:
  page-size: 28
  confirm: true

  blocks:
    "minecraft:stone": 1



debug:
  xp-blocks: false